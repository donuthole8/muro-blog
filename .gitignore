node_modules/
dist/
.DS_Store

# Symfony
# .env はコミットする（開発用の既定値のみ）。実際の秘密情報は .env.local に置く。
apps/api/var/
apps/api/vendor/
apps/api/.env.local
apps/api/.env.*.local

# フロントエンド（ADMIN_TOKEN を持つので絶対にコミットしない）
apps/web/.env
apps/web/.env.local
apps/web/.wrangler/
apps/web/.tanstack/
